import { createTheme } from "@mui/material";

const Theme = createTheme({
    palette: {
      primary: {
        main: '#000000',
      },
      secondary: {
        main: '#000000',
      },
      background: {
        default: '#f0ece4',
        paper: '#f0ece4'
      },
    },
  });

export default Theme